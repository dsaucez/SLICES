!
! =========== Managed by sonic-cfggen DO NOT edit manually! ====================
! generated by templates/quagga/bgpd.conf.j2 with config DB data
! file: bgpd.conf
!
!
!
hostname Spine-1
password zebra
enable password zebra
!
log syslog informational
log facility local4
!!
agentx
!
!
router bgp 65000
 bgp router-id 10.0.1.1
 bgp log-neighbor-changes
 bgp default ipv4-unicast
 bgp bestpath as-path multipath-relax no-as-set
 bgp bestpath compare-routerid
 bgp network import-check
 neighbor LEAF peer-group
 neighbor LEAF remote-as external
 neighbor LEAF bfd
 neighbor LEAF advertisement-interval 5
 neighbor LEAF timers 3 9
 neighbor LEAF timers connect 30
 neighbor LEAF capability extended-nexthop
 neighbor Ethernet1 interface peer-group LEAF
 neighbor Ethernet2 interface peer-group LEAF
 !
 address-family ipv4 unicast
  redistribute connected
  neighbor LEAF activate
  no neighbor LEAF send-community large
  maximum-paths 2
  maximum-paths ibgp 1
 exit-address-family
 !
 address-family ipv6 unicast
 exit-address-family
 !
 address-family l2vpn evpn
  neighbor LEAF activate
 exit-address-family
!

