!
! =========== Managed by sonic-cfggen DO NOT edit manually! ====================
! generated by templates/quagga/bgpd.conf.j2 with config DB data
! file: bgpd.conf
!
!
!
hostname Leaf-2
password zebra
enable password zebra
!
log syslog informational
log facility local4
!!
agentx
!
!
router bgp 65002
 bgp router-id 10.0.2.2
 bgp log-neighbor-changes
 no bgp default ipv4-unicast
 bgp bestpath as-path multipath-relax no-as-set
 bgp bestpath compare-routerid
 bgp network import-check
 neighbor SPINE peer-group
 neighbor SPINE remote-as external
 neighbor SPINE bfd
 neighbor SPINE advertisement-interval 5
 neighbor SPINE timers 3 9
 neighbor SPINE timers connect 30
 neighbor SPINE capability extended-nexthop
 neighbor Ethernet1 interface peer-group SPINE
 neighbor Ethernet2 interface peer-group SPINE
 !
 address-family ipv4 unicast
  redistribute connected
  neighbor SPINE activate
  no neighbor SPINE send-community large
  neighbor SPINE allowas-in 2
  maximum-paths 4
  maximum-paths ibgp 1
 exit-address-family
 !
 address-family ipv6 unicast
 exit-address-family
 !
 address-family l2vpn evpn
  neighbor SPINE activate
  advertise-all-vni
  advertise ipv4 unicast
 exit-address-family
!

